# LedgerFlow

LedgerFlow is a deterministic billing rules engine and invoice simulation toolkit for SaaS teams.

## Development

```sh
npm install
npm run lint
npm run typecheck
npm test
npm run build
```
