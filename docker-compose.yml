services:
  ledgerflow:
    build: .
    ports:
      - "3000:3000"
    environment:
      LEDGERFLOW_DB: /data/ledgerflow.sqlite
    volumes:
      - ledgerflow-data:/data

volumes:
  ledgerflow-data:
